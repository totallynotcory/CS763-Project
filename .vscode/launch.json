{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/code/peak-performace/package.json",
                "localRoot": "${workspaceFolder}/code/peak-performace"
            }
        }
    ]
}